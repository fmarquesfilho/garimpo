{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "type": "object",
  "properties": {
    "input": {
      "type": "string",
      "description": "URL ou username da loja (ex: https://shopee.com.br/loja_exemplo ou loja_exemplo)"
    },
    "cron": {
      "type": "string",
      "description": "Expressão cron para agendamento (default: a cada 8h)."
    },
    "origem_padrao": {
      "type": "string",
      "description": "Marketplace de origem padrão (ex: shopee)."
    },
    "keywords": {
      "type": "array",
      "items": { "type": "string" },
      "description": "Keywords para filtrar coleta (se vazio, coleta todos os produtos da loja)."
    }
  },
  "required": ["input"],
  "additionalProperties": false
}
