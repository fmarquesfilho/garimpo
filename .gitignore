.DS_Store

# Firebase cache
.firebase/
*.cache*.out
cloudflare-worker/.wrangler/
coverage.out

# Docs site (Rspress) — conteúdo gerado pelo sync (fonte única: docs/)
docs-site/node_modules/
docs-site/doc_build/
docs-site/.rspress/
docs-site/docs/

# Binários Go compilados
/gen-er
/gen-board
/collector
/publisher
/alerter
/scheduler

# Chaves (nunca comitar)
key.json

# ─── C# / .NET ──────────────────────────────────────────────────
src/**/bin/
src/**/obj/
src/**/.vs/
src/**/*.user
src/**/*.suo
*.DotSettings.user

# ─── Proto gerado ───────────────────────────────────────────────

# ─── Docker ─────────────────────────────────────────────────────
.env.local
firebase-debug.log
__pycache__
docker-compose.override.yml

# Go compiled binaries (local builds)
collector
publisher
scheduler
gen-board
