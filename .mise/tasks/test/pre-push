#!/usr/bin/env bash
# mise description="Validação pre-push: unit + integration + regression + parity + check + build"
# mise depends=["test:fast", "test:integration", "test:regression", "test:parity"]
set -euo pipefail

echo ""
echo "═══════════════════════════════════════"
echo "  🛡️  test:pre-push — validação completa"
echo "═══════════════════════════════════════"

echo ""
echo "── svelte-check ──"
cd web && bun run check
cd ..

echo ""
echo "── build ──"
cd web && bun run build
cd ..

echo ""
echo "═══════════════════════════════════════"
echo "  ✅ pre-push passed (ready to push)"
echo "═══════════════════════════════════════"
