#!/usr/bin/env bash
# mise description="Testes de regressão (bugs reproduzidos)"
set -euo pipefail

echo "═══════════════════════════════════════"
echo "  🐛 test:regression"
echo "═══════════════════════════════════════"

REGRESSION_FILES=(
  "src/tests/fixtures-contract.test.js"
  "src/tests/loading-timeout.test.js"
)

echo ""
echo "── Frontend (vitest — regression) ──"
cd web && bunx vitest run "${REGRESSION_FILES[@]}"

echo ""
echo "═══════════════════════════════════════"
echo "  ✅ test:regression passed"
echo "═══════════════════════════════════════"
