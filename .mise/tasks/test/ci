#!/usr/bin/env bash
# mise description="CI: pre-push + coverage (GitHub Actions)"
# mise depends=["test:pre-push", "test:coverage"]
set -euo pipefail

echo ""
echo "═══════════════════════════════════════"
echo "  ✅ test:ci passed (unit + integration + regression + parity + coverage + check + build)"
echo "═══════════════════════════════════════"
