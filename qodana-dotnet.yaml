# Qodana for .NET (requires Ultimate license — $6/contributor/month)
# Runs JetBrains Rider inspections in CI.
# Docs: https://www.jetbrains.com/help/qodana/dotnet.html
#
# NOTE: This linter requires QODANA_TOKEN. If you're on the free Community plan,
# skip this config and rely on:
#   - dotnet build (warnings-as-errors)
#   - NetArchTest (13 fitness functions)
#   - dotnet test (51 tests including architecture rules)
#
# To enable: set QODANA_TOKEN in GitHub Secrets and uncomment the CI step.

version: "1.0"

linter: jetbrains/qodana-dotnet:2026.1

dotnet:
  solution: src/Garimpei.sln

include:
  - name: All

exclude:
  - name: All
    paths:
      - src/Garimpei.Protos/Generated/
      - web/

failThreshold: 0
